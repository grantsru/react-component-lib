import * as React from 'react';
import MedContainer from './MedContainer';

export const MedicationContainer = () => <MedContainer />;

export default {
  title: 'Medication Search & Form',
  component: MedicationContainer,
};
